{
    "builds" : [{
        "src":"port/wsgi.py",
        "use":"@vercel/python",
        "config" : { "maxLambdaSize":"15md","runtime":"python12"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"port/wsgi.py"
        }
    ]
}